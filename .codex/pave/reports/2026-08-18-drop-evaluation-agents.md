# 리포트 — 전송본 기록 · ④ 촬영 주체 번역 · 평가 Agent 제거

Status: 코드 완료 · 배포 대기
Date: 2026-08-18
계획: `.codex/pave/plans/2026-08-18-drop-evaluation-agents.md`

## 한 일

| # | 결과 |
|---|---|
| 4번 전송본 기록 | 프로바이더가 `sentPrompt`(네거티브가 합쳐진 최종 문자열)를 돌려주고, 제출 직후 `generation_jobs.prompt`에 기록한다. **DB에 남는 것이 곧 전송본**이다 |
| 2번 ④ 촬영 주체 | `image-prompt-generator-v3`. 촬영 주체·지지 장치는 이름 대신 시점(방향·각도·높이·거리)으로만 옮기고, "다른 사람이 찍었다"는 사실은 사진에 남는 것(렌즈를 보지 않는 피사체, 연출되지 않은 순간, 완벽하지 않은 프레임)으로 옮긴다. `notInFrame` 규칙과의 자기모순도 함께 정리 |
| 평가 제거 | 워커 6파일 + 프롬프트 4개 + `src/admin/evaluations/` + 배선·설정·읽기 모델·화면·테스트. legacy V2 레일의 `evaluation` 단계도 제거(내용이 평가뿐이었다) |
| 문서 | §19.7 신설(제거 결정과 근거), §19.3 1차 증거 규정 정정, §19.6 "④ 잘못이 아니다" 정정, research-log 관측 4 정정 |

## 정정된 두 주장

1. **관측 4**: 서린이 통과한 이유를 "페르소나 블록 중복"으로 적었으나, 더 큰 원인은
   `visualProfile.stylePrompt`에 촬영 매뉴얼이 통째로 들어 있는 것이었다. register의
   실질 소유자는 ③가 아니라 캐릭터 전역 설정 한 줄이다.
2. **§19.6**: "④ 잘못이 아니라 ③가 좌표만 줬기 때문"은 반례가 같은 표본에 있었다.
   ③가 주체를 명시한 3컷에서 ④가 3/3 삭제했다.

## 검증

`npm test` 46스위트 412건 · `npm run admin:check` 16파일 48건 · `npx tsc --noEmit`
· `npm run lint` · prettier 전부 통과. 스키마 무변경(마이그레이션 없음).

신규 테스트 1건(Test Value Gate): **전송본이 잡에 기록되는가** — 회귀하면 실제
나간 프롬프트를 다시 못 보게 되고 연구 로그의 1차 증거가 또 틀려진다.

## 남긴 것 (의도적)

- `DraftEvaluation`·`EvaluationReport` 테이블과 기존 행, `evaluator.*` 설정 값
- `src/worker/prompt-lint.ts` — 호출처 없음. 파일 상단에 그 사실과 재사용 시
  주의(오작동 2건)를 주석으로 남겼다

## 남은 일

1. 배포(admin만. 스키마 변경 없음)
2. 데이터: 캐릭터 3명 `capture_style` 블록, 장소 4곳 네거티브 이관 — **아직 안 했다.**
   이게 없으면 v2 계약의 효과가 안 나타난다
3. 관측: 3캐릭터 재생성

## 잔여 위험

- 다음 관측은 **v2 계약 + v3 생성기 + 평가 없음** 세 변경이 겹친다. 개별 기여도
  판정 불가
- 검증이 운영자 판정뿐이다(§19.7)
